import { OPENAI_API_KEY } from '@env';

export const config = {
  apiKey: OPENAI_API_KEY,
  apiEndpoint: 'https://api.clean-home.com/api/assistant',
};

export const API_URL = 'http://localhost:3000'; // Update this with your actual backend URL 